import { createContext } from 'react';

export const MyContext = createContext();
export const Page = createContext();
export const PagesCount = createContext();
export const Genre = createContext([]);

